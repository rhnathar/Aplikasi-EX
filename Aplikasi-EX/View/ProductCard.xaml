<UserControl x:Class="Aplikasi_EX.View.ProductCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Border Background="White" CornerRadius="5" Padding="10" Margin="5" BorderBrush="LightGray" BorderThickness="1">
		<Border.Effect>
			<DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="8" ShadowDepth="3"/>
		</Border.Effect>

		<StackPanel>
			<Image Source="{Binding Image}" Width="180" Height="150" Stretch="Uniform"/>
			<TextBlock Text="{Binding ProductName}" FontWeight="Bold" Margin="0,5,0,0" />
			<TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0" />
			<TextBlock Text="{Binding Price, StringFormat=C}" FontWeight="Bold" Foreground="Blue" Margin="0,5,0,0" />
			<Button Content="Detail" Command="{Binding DataContext.NavigateToDetailCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
					CommandParameter="{Binding}" Background="Transparent" BorderThickness="1.25" Foreground="#1E4AE9" Margin="0,5,0,0"
					BorderBrush="#1E4AE9" Height="25"
					/>
		</StackPanel>
	</Border>
</UserControl>
